import type { Transition } from 'framer-motion'

export type LogCarouselPage = 0 | 1 | 2 | 3

/** Carousel slide `transition` prop: same as Framer’s `Transition` (see `motionTransition` / reduce-motion). */
export type LogFormSlideTransition = Transition
